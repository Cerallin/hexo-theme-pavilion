- const meta = post_meta(page)

.book-info
    .book-image
        img(src= meta.thumbnail, alt= meta.title)
    .book-info-box
        .book-title-box
            .book-date= meta.date
            h2.book-title= meta.title
        .book-authors
            each author of meta.authors
                .author-name= author
